---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="flex flex-col gap-4">
  <span class="text-[var(--text-inverse)] text-[15px] font-semibold">{title}</span>
  <slot />
</div>
